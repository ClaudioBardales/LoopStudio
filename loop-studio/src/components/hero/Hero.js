import React from 'react';
import Styled from 'styled-components';
import Navbar from '../Nav/Navbar';

const Hero = () => {
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default Hero;
